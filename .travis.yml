language: node_js
node_js:
  - 16
services:
  - docker
before_install:
  - docker-compose -f docker-compose-build.yaml build --parallel
  # Do similar for other three images
script:
  # Tagging
  - docker tag udagram-api-feed ntpac/udagram-api-feed:v2
  - docker tag udagram-frontend ntpac/udagram-frontend:v2
  - docker tag udagram-api-user ntpac/udagram-api-user:v2
  - docker tag reverseproxy ntpac/reverseproxy:v2
  # Do similar for other three images
  # Push
  # Assuming DOCKER_PASSWORD and DOCKER_USERNAME are set in the Travis repository settings
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push ntpac/udagram-api-feed:v2
  - docker push ntpac/udagram-frontend:v2
  - docker push ntpac/udagram-api-user:v2
  - docker push ntpac/reverseproxy:v2